"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
function HighlightQuery(text, query, style) {
    var others = [];
    for (var _i = 3; _i < arguments.length; _i++) {
        others[_i - 3] = arguments[_i];
    }
    if (query === null ||
        query.length === 0 ||
        text === null ||
        text.length === 0) {
        return React.createElement("span", null, text);
    }
    var defaultStyle = { fontWeight: "bold" };
    var startingIndex = text.toLowerCase().indexOf(query.toLowerCase(), 0);
    return startingIndex > -1 ? (React.createElement("span", __assign({}, others),
        text.substring(0, startingIndex),
        React.createElement("span", { style: style ? style : defaultStyle }, text.substring(startingIndex, startingIndex + query.length)),
        text.substring(startingIndex + query.length))) : (React.createElement("span", null, text));
}
exports.HighlightQuery = HighlightQuery;
